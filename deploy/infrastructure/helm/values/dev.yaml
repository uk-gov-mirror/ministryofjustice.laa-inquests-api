# Default values for laa-inquests-api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: false
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: "cd-serviceaccount"

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  capabilities:
    drop:
      - ALL
  seccompProfile:
    type: RuntimeDefault

service:
  type: ClusterIP
  port: 8027

ingress:
  enabled: true
  className: modsec-non-prod
  hosts:
    - host: laa-inquests-api-dev.apps.live.cloud-platform.service.justice.gov.uk
      paths:
        - path: /
          pathType: Prefix
  annotations:
    external-dns.alpha.kubernetes.io/set-identifier: "laa-inquests-api-laa-inquests-api-dev-green"
    external-dns.alpha.kubernetes.io/aws-weight: "100"
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/enable-owasp-core-rules: "true"
    nginx.ingress.kubernetes.io/modsecurity-transaction-id: "$request_id"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
      SecRuleEngine On
      SecDefaultAction "phase:2,pass,log,tag:github_team=laa-inquests"
      SecAction "id:900200,phase:1,nolog,pass,t:none,setvar:tx.allowed_methods=GET HEAD POST OPTIONS PATCH"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

livenessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 30
  periodSeconds: 60

readinessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 30
  periodSeconds: 60

env:
  AWS_SECRETS_INQUESTS_API_ENTRA_CONFIG: ""
  AWS_SECRETS_GOV_NOTIFY_API_KEY: ""
  AWS_SECRETS_GOV_NOTIFY_TEMPLATE_IDS: ""
  AWS_SECRETS_GOV_NOTIFY_CALLBACK_BEARER_TOKEN: ""
  AWS_SECRETS_SDS_BASE_URL: ""
  AWS_SECRETS_SDS_TENANT_ID: ""
  AWS_SECRETS_SDS_CLIENT_ID: ""
  AWS_SECRETS_SDS_CLIENT_SECRET: ""
  AWS_SECRETS_SDS_SCOPE: ""
  SERVICE_NAME: "Python Fastapi Template"
  SERVICE_PHASE: "Beta"
  DEPARTMENT_NAME: "Legal Aid Agency"
  DEPARTMENT_URL: "https://www.gov.uk/government/organisations/legal-aid-agency"
  CONTACT_EMAIL: "eligibility@justice.gov.uk"
  CONTACT_PHONE: ""
  SERVICE_URL: "https://www.gov.uk/legal-aid"
  SESSION_NAME: "sessionId"
  RATELIMIT_HEADERS_ENABLED: true
  RATELIMIT_STORAGE_URI: ""
  RATE_LIMIT_MAX: 10000
  RATE_WINDOW_MS: 900000
  NODE_ENV: "dev"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}
